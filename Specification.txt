SPECIFICATION.

Entity:
    Article (table name = articles)
        Descriptions:
            Describes the essence of a specific article
        Properties:
            id (column name = id)
            title (column name = title)
            subUrl (column name = sub_url)
            coverImage (column name = cover_image)
            category (ManyToOne, join column name = category_id)
                ref to categoty entity
            tags (ManyToMany join column name = tags_id)
                ref ti tags entity
            status (column name = status)
            views (column name = views)
            enableComment (column name = enable_comment)
            isDeleted (column name = is_deleted)
            createTime (column name = create_time)
            updateTime (column name = update_time)
            content  (column name = content)


